# Next session prompt

The fenced block below is the hand-off. It is published to the Desktop by
`python tools/publish_next_session.py`, which reads THIS file and refuses on
anything other than exactly one fenced block, on non-ASCII, or on a block under
2000 bytes. Edit the block here; never retype it anywhere else.

```
RSC - next session, HEADLESS, operator AWAY.

Repo: C:\Resin Compute (github.com/Remus3/Resin-Compute). Counterparty: RC.
CS, LW and LL are on ORDERED STANDBY - silence is NOT dissent, do not write
into their trees.

READ FIRST: CLAUDE.md, README.md, docs/SPEC_SCAFFOLD.md, ROADMAP.md,
docs/LEDGER.md, git log. Before planning any character build read
docs/GOAL_SPEC_SEED_TEAM.md. Before adding a data source read
docs/LICENSE_NOTES.md. Before re-litigating read docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS. Verified in docs/SPEC_SCAFFOLD.md section 3,
corrections in ADR-003. 50/50 is 55.000% consolidated since 5.0. Weapon
soft-pity saturates at pull 77 under a 7% increment. 1.600% is 1/E[wishes per
5-star], NEVER a per-wish Bernoulli parameter.

OPERATOR AWAY. NO BLOCKING QUESTIONS. Where you would escalate, dispatch an
ADJUDICATOR and record the ruling in ROADMAP.md MARKED PLAINLY AS AN
ADJUDICATED CALL. NINE stand. THIS ENUMERATION IS THE SOURCE and the number is
a claim about it - if they disagree, the list wins and you fix the number:
  1. the conftest non-128 disposition
  2. the 2026-09-09 MIXED ruling on the os.name-forcing arms
  3. the do-not-widen ruling on the mkdir ValueError sites
  4. the OVERTURN of that ruling for ONE site only
  5. the MIXED ruling on the dev-pin guard shape
  6. the gate REFERENT, broad reading
  7. slice B as MERGED-REDUCED
  8. the headless loop's HALT BOUNDARY, MIXED, 2026-09-09, ROADMAP.md:67
  9. atomic_write_text KEEPS RAISING UnicodeEncodeError, 2026-09-09,
     ROADMAP.md:39
Read the paragraph before touching any of them. The previous hand-off said
SEVEN and enumerated seven; this session added TWO.

DO NOT ARM ANYTHING. RC disarmed, hop budget spent, task Disabled. Verified
DORMANT this session via ops/check_task_liveness.py, exit 1, trigger expired
2026-09-07T21:00. NOTHING WAS ARMED.

GATES, in /done order, EACH AS ITS OWN COMMAND. Chaining with && reports only
the LAST link.
  python -m pytest tests/test_licence_posture.py
  python -m pytest tests/test_docs_consistency.py
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
DO NOT PASS -q ON THE COMMAND LINE. pytest.ini already sets -q; a second makes
it -qq and suppresses the count.

STATE AS OBSERVED 2026-09-09 at a62f34c, a reading not a promise. RE-MEASURE.
  pytest tests   1867 passed, 1 skipped     agents/pity_engine   80 passed
  node --test    52 pass 0 fail             headless / ruff / qa / licence  exit 0
  licence 47, docs consistency 29, docs hook commands 17, qa 16 passed 0 failed
  2 skipped 3 noted. mypy 34 source files - ADVISORY, NOT A GATE.
  Arithmetic: previous reading 1859 passed 1 skipped at 3533965.
  1859 + 7 (atomic io) + 1 (census population arm) = 1867, closes.
  CHECK EVERY SESSION, FIRST: gh run list --limit 5
  CI at a62f34c: docs-guards GREEN. The `ci` workflow was STILL IN PROGRESS
  when this was written. That is the reading. DO NOT treat it as green.
  THE SAME SUITE REPORTS A DIFFERENT SKIP COUNT UNDER THE PRE-PUSH HOOK, and
  it is NOT a regression. Measured at a62f34c: 1867 passed 1 skipped from an
  ordinary shell, 1866 passed 2 SKIPPED under pre-push. The extra skip is
  tests/test_hook_interpreter.py:1802 - under the hook, git is invoked as the
  git-core shim at Git\mingw64\libexec\git-core\git.exe, which sits at no
  layout that oracle recognises as an install, so it skips rather than
  guessing. The second skip is the opt-in network one. DO NOT reconcile the
  two counts by editing either; they are two environments and the skip reason
  says so.

1. TWO NEW STANDING OPERATOR DIRECTIVES, 2026-09-09, now in CLAUDE.md.
   SUBAGENT-FIRST ALWAYS - the main session is the OPERATOR'S SURFACE and must
   stay quiet and clear. The main thread holds the plan, the seams, the merge
   and the report, and nothing else. No long suites, no wide sweeps, no builds
   inline.
   THE RESPONDER PROGRAM'S WORK RUNS ON AN UNATTENDED HEADLESS LOOP that halts
   and pings at a defined boundary. THE LOOP IS NOT YET BUILT. This session
   recorded the directive and the boundary. It did NOT build a loop, and no
   file in ops/loop/ was created by it.

2. THE HALT BOUNDARY IS AN ADJUDICATED CALL, MIXED. Verbatim, so a loop author
   does not re-derive it. HALT AND PING BEFORE:
     (a) any write, delete, unlink or named-kernel-object acquisition whose
         target path or namespace is OUTSIDE THIS REPO ROOT - explicitly the
         machine-wide slot bucket under C:\ProgramData and the Global mutex
         namespace;
     (b) any commit or push whose diff touches ops/loop/slots.py or
         ops/loop/winmutex.py, or that trips the sibling-name sweep;
     (c) any arming, agreement, or behaviour change in ANOTHER PARTY'S TREE.
   AND NOT BEFORE an ordinary push that passes both suites and the sweep with
   RESIN_SKIP_PREPUSH unset.
   THE UNRULED RESIDUAL IS THE NO-ANSWER RULE. Three of five participants are
   on ORDERED STANDBY and CANNOT REPLY, so bilateral arming is unreachable.
   Timeout-plus-default-deny was PROPOSED AND NOT ADOPTED. Park the item.
   Record standby, and NOT dissent.
   RC ADOPTED THE BOUNDARY and withdrew its own ARMING candidate. THE NO-ANSWER
   RESIDUAL IS NOW ANSWERED: NO TIMEOUT, HALT AND WAIT, and that answer is
   ADOPTED HERE. Timeout-plus-default-deny is WITHDRAWN, marked in place in
   ROADMAP.md rather than deleted.
   ONE DISAGREEMENT REMAINS OPEN. RC's adopted form pre-authorises a push to
   RC's OWN origin. THAT CARVE-OUT IS SEAM (f) ITSELF. RSC's position is to
   GATE THE DIFF CONTENT, NOT THE DESTINATION, which is what clause (b) already
   does. Not settled - do not record it as agreed.

3. STEP THREE OF THE GATE CENSUS IS NOT THE REGISTRY AS BRIEFED - a planner
   REFUTED the brief and was right. 13 of 17 adjacent gate-name swaps leave
   the census GREEN. Rotating all 18 names leaves EVERY problem list empty.
   Only 3 of 18 names are bound at all, and by accident.
   THE RIGHT ARTIFACT IS A NAME-TO-SITE BINDING TABLE, closing CEILING item 1.
   THE TRAP: 34 of 35 mutants DESTROY THEIR OWN ANCHOR, so a binding table
   shipped as a test module becomes a THIRD SHAPE GRADER, reporting 34 syntax
   reddenings as KILLED at exit 0. verify_exclusions is BLIND to it because it
   iterates the DECLARED list only.
   KEEP THE TWO EXCLUSIONS AND THEIR TWO DIFFERENT REASONS UNMERGED.
   SELF_TEST_MODULE is a module that decides its own verdict.
   SHAPE_GRADER_MODULES is a module that grades the TARGET FILE'S SHAPE, which
   answers a syntax question while the runner asks a behaviour one. An
   --ignore of a MISSING path is a SILENT NO-OP.
   THE ANCHOR PROPOSAL: a HAND-TYPED substring required present AND UNIQUE
   inside _run_once. All 18 are unique today. ITS FAILURE MODE IS THAT THE
   REPAIR TEMPTATION IS TO TRIM THE ANCHOR UNTIL IT MATCHES - the widening
   this tree has been defeated by three times. Do not trim; re-anchor.
   The planner's six-slice decomposition lived in a SCRATCH DIR THAT DOES NOT
   SURVIVE THE SESSION. It must be RE-DERIVED, not recalled.

4. THE HAND-TYPED COUNT CLASS FIRED THREE TIMES IN ONE SESSION, and an
   ADVERSARIAL PASS REPRODUCED THE ERROR INSTEAD OF CATCHING IT - it read a
   SUMMARY SENTENCE rather than counting the enumeration below it.
   THE RULE: when a document states a count AND ALSO ENUMERATES the thing
   counted, THE ENUMERATION IS THE SOURCE and the sentence is a claim about
   it. Every counting sentence MUST NAME ITS POPULATION - whose enumeration,
   of what, measured when. A bare number is not a measurement.

5. EIGHT TIMES AN AGENT CORRECTED THE INSTRUCTION IT WAS GIVEN and EVERY
   CORRECTION HELD. That is up from four the session before, same direction.
   THE STANDING LESSON: WHEN A REFUTER SAYS A BUILDER WAS WRONG, CHECK WHO
   WROTE THE INSTRUCTION FIRST.

6. OPEN ROWS, each with its measurement in ROADMAP.md. RE-PROBE BEFORE
   SPENDING A SLICE - a row is a claim with a date and it DECAYS.
   - moon_sync_inbox IS GITIGNORED WHOLESALE. .gitignore:115, and
     `git ls-files moon_sync_inbox` returns 0 against 152 files on disk. SO NO
     GLYPH GATE, LICENCE GATE, LINE-ENDING GATE OR DOCS GUARD READS ANY NOTE,
     inbound or outbound. THE MECHANISM: the guards derive their corpus from
     git ls-files, not from the universal. A green gate cited about a note is
     green about THE TREE and says nothing about the note.
   - The evidence ledger TRIMS rather than rotates (tools/moon_sync_responder.py:1037).
   - Which ledger instance is live has never been CHECKED here.
   - Nothing guards the SENDER side against editing a note after delivery.
   - The sweep floor's VALUE is ungraded: _MIN_TRACKED_PATHS 100 -> 10 leaves
     the suite green. Pre-existing blindness, not a regression.
   - Half of tests/test_conftest_skip_path_pinned.py is DEAD as shipped.
   - Nothing inspects the pre-push hook's OUTPUT. The arms are a token scan.
   - ops/ResinCompute-Supervisor.xml never registered, Exec argv graded by
     nothing.
   - THREE hand-typed transcriptions of one reason wording exist and NOT ONE
     derives from a CONSUMER's stated requirement.
   - UNGRADED AND HONEST ABOUT IT: the census collision arities are a SAMPLE
     not a proof. The depth arm bounds reported[:n] only for n BELOW the tag
     count. Kills attributed by parsing the FIRST failure under -x name the
     killer but do not prove no other test would also have failed.
   - PRE-EXISTING AND NOT OURS: in a git archive extract the whole suite is
     EXIT=1 for the BASELINE too. An extract and a shallow clone are NOT one
     population. Stale worktrees accumulate under .claude/worktrees/.

7. NEW ADJUDICATED CALL, and it is on the list of nine above.
   atomic_write_text KEEPS RAISING UnicodeEncodeError. It is NOT fail-soft for
   a payload the encoder rejects, and that is deliberate: a silent coercion
   would write bytes nobody authored. ROADMAP.md:39. Do not "fix" it.

SESSION SHAPE is orchestrated, multi-agent, self-adjudicating, self-adversarial
by DEFAULT, and now SUBAGENT-FIRST ALWAYS. The agent that produced a thing
NEVER grades it. FREEZE THE CANDIDATE before dispatching a grader. Spawn
refuters with DISTINCT LENSES. AGREEMENT BETWEEN TWO AGENTS IS NOT EVIDENCE -
find their shared input and test THAT.

THE METHOD LESSONS THAT PAID OFF MOST.

- A SHAPE ARM PINS FORMAT, NOT INPUT. An arm proving a conjunct is welded
  pinned a phrase the message emits UNCONDITIONALLY, so a coincidental proxy
  passed all 69. The repair defeated ONE literal and a further lens found
  SEVEN more conjuncts passing all 71, failing in OPPOSITE directions. PIN
  IDENTITY AT MORE THAN ONE ARITY, and always include an OVER-FIRE control.
- A SHAPE GRADER KILLS ITS OWN MUTANT. The campaign was wrong twice and both
  errors were found by MEASUREMENT, never by reading a report. First run said
  35/35 KILLED and measured NOTHING - the tell was the WALL CLOCK. Second run
  had FOUR FALSE KILLS because the census grades an AST SHAPE.
- DISTINCT LENSES FIND DISJOINT DEFECT CLASSES. Vacuity, prose-truth,
  false-green, does-it-reproduce, equivalent-mutant. The reproduce lens found
  the four false kills and nothing in any report could have shown it.
- A MODULE'S WRITTEN CLAIM ABOUT ITS OWN LIMITS IS A TESTABLE ASSERTION.
- THE DOCS GUARDS CATCH THE MERGER EVERY SESSION. Run test_docs_consistency.py
  AND test_docs_hook_commands.py after ANY doc edit.

TRAPS THAT ACTUALLY BIT, every one measured.

- A WORKTREE MATERIALISES AT AN ANCESTOR OF THE DECLARED FORK POINT. Put an
  explicit `git merge --ff-only <sha>` in every brief PLUS an assertion on the
  materialised bytes - a line count and a grep for a symbol that exists only
  at the intended commit. A dispatch without it is the defect, not the builder.
- UNCOMMITTED SLICE WORK IS INVISIBLE TO THE NEXT WORKTREE. A repair builder
  must be told to `cp` the predecessor's files in by absolute path.
- A NET-ZERO-BYTE-SIZE mutate-and-restore WITHIN THE SAME SECOND runs the
  MUTANT'S BYTECODE against restored source. Purge __pycache__ AND
  .pytest_cache on BOTH sides; prefer a replacement of different length.
- NEVER `git checkout --` to restore a mutation. Restore from saved bytes and
  verify by sha256.
- `assert source.count(old) == 1` before substituting.
- A QUOTED HEREDOC STILL MANGLES BACKSLASHES. Write .py with the editor tools.
- write_text EMITS CRLF ON WINDOWS and .gitattributes eol=lf hides it. tests/
  is OUTSIDE test_no_crlf_writers.py's corpus, so nothing catches it there.
- grep -c '[^\x00-\x7F]' IS NOT A CHARACTER CLASS. It reported 1785 non-ASCII
  lines in a file with ZERO non-ASCII bytes. Check bytes IN PYTHON.
- SendMessage IS DISABLED. A running agent CANNOT be amended - dispatch a new
  disjoint slice.
- AN AGENT CAN LOOP ON STALE `sleep` WAIT-BLOCKS AFTER FINISHING, burning
  tokens and re-notifying. TaskStop it once its report has landed.
- A NEW TEST FILE MUST BE `git add`ed BEFORE test_docs_consistency.py passes
  once ROADMAP.md backticks it - the guard checks git-tracked.
- The default `python` here is 3.14, not 3.11. CI runs 3.11.
- An empty pytest parametrize is `1 skipped`, exit 0, NOT a failure.
- pytest.raises(Exception) DOES NOT CATCH Skipped - BaseException-derived.
- A FLOOR IN A SEPARATE ARM LEAVES THE PRIMARY ARM VACUOUS. One assertion.
- A PYTEST PROGRESS LINE IS 72 DOTS, NOT THE WHOLE RUN.
- exit=$? AFTER A PIPE READS THE LAST PIPE STAGE. Redirect to a file.
- A WALL-CLOCK ARM FAILS BY TIMEZONE FIRST. Green here, red on CI, same
  commit: diff the CLOCK before the environment.
- mypy files= covers core/, engines/, ingest/, agents/pity_engine/, tools/
  ONLY - its Success is ZERO OUT OF ZERO about tests/ or scripts/.
- NEVER NAME A SIBLING PROJECT IN PLAIN TEXT. RC, CS, LW, LL, RSC.
- Never Stop-Process. taskkill //F //PID under Git Bash.
- NEVER ADD A Co-Authored-By TRAILER, and never file its absence as a defect.

CHECK TASK LIVENESS WITH THE CHECKER, NEVER A STATE STRING, NEVER THROUGH A PIPE:
  python ops/check_task_liveness.py ResinCompute-Responder
Exit 0 LIVE, 1 DORMANT, 2 ABSENT, 3 UNKNOWN, 4 AMBIGUOUS. Observed 1 DORMANT.
KILL SWITCH: powershell -ExecutionPolicy Bypass -File .\ops\install_responder_task.ps1 -Remove

INBOX: 5 notes moved this session - 2 received from RC, 3 sent to RC. ZERO
subdirectories at a62f34c. RE-MEASURE THE FILE COUNT; do not trust this one.
Reading is not acknowledging - `python scripts/watch_inbox.py` reports and
`--mark` acknowledges, and marking an untriaged batch is worse than not marking.

DO NOT REDO: the core/atomic_io.py 0-byte temp leak (CLOSED e2aad97, 7 arms).
The census "0 tags anywhere else" prose decay (CLOSED cd712de). The EIGHT
GATES EXERCISED BY NOTHING (CLOSED 743a7b7, campaign reaches 35 of 35). The
slots.py re-pin (closed 71fa2a68). Manifest-as-key (withdrawn). The caveman
dialect question (settled). Wenyan (reverted 2026-06-27). Do not hunt mutants
M1-M11 - only M12 exists. Do not re-derive the two ceilings in docs/LEDGER.md.
Do not re-litigate the nine adjudicated calls without reading them first. Do
not re-sweep the mkdir sites. Do not rebuild the git-reason hand-typed
fixture. Do not re-open the census REFERENT - _is_a_consult_site was
adjudicated at c372861 and WIDENING A MATCHER is the response this tree has
been defeated by three times.

Keep chat under 500 output tokens, CAVEMAN ULTRA, 7-bit ASCII, no em-dashes, no
en-dashes, no smart quotes.
```
