"""PityEngine test suite.

Run as its own unit, matching Riot Commander's dual-suite convention:

    python -m pytest agents/pity_engine -q

Never under a root-wide `pytest .` - `pytest.ini` says why.
"""
