RESINCOMPUTE - NEXT SESSION HAND-OFF
Stamped 2026-09-20T23:41:45Z, on the round-B branch whose base commit is
ef17cc8 - NOT on main. Everything below is a READING taken at that instant, not
a promise about now. Re-measure before citing.

READ THIS BEFORE YOU TRUST A WORD BELOW. THIS FILE IS UNGUARDED, AND NOT BY
ACCIDENT OF ANYONE'S CARE. tests/test_docs_consistency.py builds its citation
corpus from _tracked_markdown(), which filters on name.endswith(".md"). This
file is tracked, is the first thing a cold session reads, and is a .txt - so
every backticked-path check, every line-citation check and every pointer check
in that module is blind to it BY SUFFIX. A SUFFIX-KEYED GUARD WITH A
SUFFIX-EXCLUDED DEFECT. It has already cost once: this file spent a session
telling readers that round B was open, highest-priority work, after round B had
landed, and nothing in the tree could have gone red over it. Treat every claim
here as hand-maintained prose, because that is all it is. A claim that must be
guarded has to live in a .md file or in a test.

BOOTSTRAP - READ IN THIS ORDER
  CLAUDE.md
  README.md
  docs/SPEC_SCAFFOLD.md
  ROADMAP.md
  docs/LEDGER.md
  git log --oneline -10
Before planning any character build: docs/GOAL_SPEC_SEED_TEAM.md.
Before adding any data source: docs/LICENSE_NOTES.md.
Before re-litigating a past choice: docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS from memory or from a web search. They are
verified and recorded in docs/SPEC_SCAFFOLD.md section 3, with corrections in
docs/adr/ADR-003-forecaster-model.md. Three are regression-tested and easy to
get wrong: the 50/50 has been 55.000 percent consolidated since version 5.0,
not 50; the weapon soft-pity ramp saturates at pull 77 under a 7 percent
increment, so any claim of 79 or 80 is arithmetically impossible; and 1.600
percent is a long-run average, never a per-wish Bernoulli parameter.

THE GATES, in the order /done runs them
  python -m pytest tests/test_licence_posture.py
  python -m pytest tests/test_docs_consistency.py
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
  python tools/publish_next_session.py
Never pytest . from the root; the two suites run separately. NEVER pass a
command-line -q - pytest.ini already carries one and a second makes -qq, which
prints no summary line at all. mypy is ADVISORY and traverses only mypy.ini's
files= roots, so it says NOTHING about headless/, ops/, surface/, scripts/ or
tests/.

STATE OBSERVED 2026-09-21 AT 7fbbaf0
  tests 3092 passed 4 skipped; pity_engine 80; shell node 52; ruff clean;
  mypy 38 source files; licence 47; docs 34; qa 17 passed 2 skipped 3 noted;
  headless smoke rc 0. Working tree clean.

  THE FOURTH SKIP IS NEW AND IS DELIBERATE. It is an arm in
  tests/test_interpreter_pinning.py declining to report an unearned green on a
  host where a bare python already resolves to sys.executable. It is not
  contamination. Compare skip REASONS with -rs, never skip COUNTS.

THE INTERPRETER QUESTION IS SETTLED - DO NOT REOPEN IT AS AN OPEN CHOICE
The Python 3.11 pin STANDS. It was settled by measurement, not by amendment.
An equipped 3.11.9 agrees with 3.14.4 on both suites, ruff, mypy, qa_companion
and the headless smoke, on an identical collection total, with exactly one
Windows-only pathlib difference in a positive-control arm whose own docstring
documents the asymmetry and which is expected green on CI's Linux 3.11. What
was never true is that this tree's figures ARE 3.11 figures: python here
resolves to 3.14.4 and CI runs 3.11.16. CLAUDE.md now says so.

WHAT LANDED - five commits, c4cf858..7fbbaf0, all pushed
  4bd3c29  Every roster numeral in docs/CHANNEL.md is derived from the parsed
           roster table. 19 sites, 20 numerals, two axes that never mix, plus 9
           must-stay-at-five exemptions pinned so a later reader finishing the
           job goes red. Two ordinal sites are graded on ABSENCE of the stale
           wording and demand a human rewrite, because the mechanical
           substitution silently drops the tree the sentence already named.
  abfa3d2  Every carrier claim names its population, and a new guard,
           tests/test_carrier_population_prose.py, reds on a directory-scoped
           or unnamed one. 18 hits on the old bytes, 0 on these.
  5d51bc1  tools/corpus_statement.py publishes untracked_not_ignored so a guard
           can STATE its corpus. It WARNS and never refuses, and nothing in it
           raises to its caller, because it runs from a git hook.
  709cfe0  tests/test_mypy_scope.py launches sys.executable, and the detector
           now FOLLOWS THE NAME through module-level bindings so a splatted
           tuple cannot hide a bare interpreter.
  7fbbaf0  The docs.

THE ONE FINDING THAT REPEATED IN ALL FOUR SLICES - read before writing an arm
Every slice shipped an arm that COULD NOT FAIL, and the cause was always one of
two shapes: THE FIXTURE EXCLUDED THE DEFECT, or THE DETECTOR COULD NOT SEE THE
SHAPE THE FIX ITSELF CREATED.
  - A helper declared two git variables inert. Its fixture had NO TRACKED FILES,
    so they had nothing to relabel. With a tracked file present both hijack.
  - An interpreter fix introduced a module-level tuple, and its detector read
    argv[0] at the call site, so reinstating the exact defect passed all 14 arms.
  - A roster tie covered 8 sites and called the rest illustrative; 8 more were
    found, then 2 more again by a census built BLIND from the document.
  - A repair declined to build a guard as necessarily vacuous. The guard was
    then built in about 20 lines and discriminated 18 to 0.
ALWAYS reinstate the exact defect and prove the arm reds. An arm that has never
rejected anything is not a gate.

TRAPS MEASURED IN THIS TREE - EACH ONE HAS ACTUALLY BITTEN
  - A VIRTUALENV ON PATH CONTAMINATES THE SUITE FIGURE. It turned a pass into a
    skip and produced a false 3002/4 that was written up as a correction to this
    file. This file was right. Re-measure in a clean shell before believing any
    figure that disagrees.
  - WINDOWS CreateProcess RESOLVES A BARE NAME against the calling image's
    directory FIRST, then cwd, then PATH - not PATH order. One process can reach
    three different interpreters and none need be the one running the suite.
  - A GUARD WHOSE CORPUS COMES FROM git ls-files CANNOT SEE AN UNTRACKED FILE.
    git add a new file BEFORE running the seam gates or the green is vacuous.
  - A NO-OP MUTANT READS AS A KILL. An adversary credited an arm with killing a
    sorted-to-list mutation; git ls-files already emits byte-sorted output, so
    that mutation is undetectable against any real fixture. Confirm a mutation
    CHANGED THE BYTES and that the kill names a specific arm.
  - grep -c RETURNS EXIT 1 ON A ZERO COUNT, which breaks an && chain and can
    silently truncate a measurement. A zero is often the interesting answer.
  - tr -d with a NUL deletes the separator and collapses everything onto one
    line, so a count comes back as 1. Translate the NUL to a newline instead.
  - THE DOCS GUARD REFUSES A BACKTICKED PATH GIT DOES NOT TRACK. Write
    gitignored, sibling-only and nonexistent paths in PLAIN TEXT.
  - A LINE-NUMBER CITATION INTO A DOC DECAYS, AND THIS ENTRY IS ITS OWN PROOF.
    It used to say CLAUDE.md cites tests/test_loop_concurrency.py:145-146. By
    the time anyone read that, CLAUDE.md said 156-157; inserting one import
    line then moved the pin again and CLAUDE.md now says 157-158. Three values
    in three sittings, with only a RANGE check guarding any of them - and a
    range check cannot tell a right line from a wrong one inside the same file,
    so every one of those readings stayed green. CLAUDE.md now names the
    SHARED_SHA256 symbol beside the number so a reader who finds it shifted can
    re-locate. VERIFY THE NUMBER BEFORE YOU REPEAT IT; do not carry it forward.
  - Path.write_text emits CRLF on Windows and eol=lf in .gitattributes hides it
    from every diff. Byte-copy vendored artifacts; shutil.copyfile, never a
    text-mode write.
  - A quoted heredoc still mangles backslashes, and a long one can fail to parse
    outright. Use the Write tool for anything substantial.
  - The commit message file goes in the SESSION SCRATCHPAD, never a shared tmp
    path. Under Git Bash a /tmp redirect lands in the Git INSTALLATION dir.
  - Under Git Bash write taskkill //F //PID <pid>. Never Stop-Process.
  - cd inside a compound command DRIFTS the working directory for later calls.
  - Writing an outbound note into THIS tree's own inbox is AUTHORING, not
    DELIVERING. Verify by reading the RECIPIENT'S copy.
  - Subagents contradicted this session's briefs on measured points repeatedly
    and were right every time, including about the briefs' own control figures.
    Brief them to trust their own probe.

OPEN WORK, HIGHEST PRIORITY FIRST - full text in ROADMAP.md, cited by heading
  1. ROUND B IS LANDED. DO NOT DISPATCH ANYONE AGAINST IT. This item used to
     read "ROUND B LANDS 2026-09-22 AND RSC OWES ITS PART", and it was already
     false when written - a cold session reading it would have been sent to do
     work that exists. It landed in commit ef17cc8, "fix(loop): land round B on
     winmutex.py and move every pin with it". ops/loop/winmutex.py moved from
     0b112a4f..., 6190 bytes, to
     df0a7a40c28818130dfde25144c971c06060b4645e5eb5f679fbdaf55e2e08d7, 6184
     bytes - ONE comment line reworded, no behaviour - with the sha256 pin in
     tests/test_loop_concurrency.py, the digest in docs/LICENSE_NOTES.md and
     _KNOWN_CHANNEL_CODE_VIOLATIONS in tests/test_no_sibling_names.py all
     moving in that one commit. ops/loop/slots.py did NOT move in round B.

     WHAT ACTUALLY REMAINS, and it is short:
       a. ef17cc8 is on a WORKTREE BRANCH, not on main. The mainline checkout
          still has 0b112a4f, 6190 bytes, on disk. Until a merger lands the
          branch, anyone who hashes the repository root gets the SUPERSEDED
          digest and is not wrong to. Say which you measured.
       b. CARRIERS STILL OUTSTANDING, measured 2026-09-20T23:41:45Z by hashing
          each root's own disk: SS still holds 0b112a4f, 6190 bytes. LW and RC
          have landed df0a7a40 and are done. CS is not outstanding and never
          will be by this route - its copy is a DIFFERENT FILE at e0d3ac7d,
          7724 bytes, divergent before round B and untouched by it. LL carries
          no copy. THAT ROW IS A SNAPSHOT OF FOREIGN DISKS AND DECAYS: it was
          measured minutes after a verifier had checked a different answer.
          Re-hash before citing it. The roster and its stamp live in
          tests/test_loop_concurrency.py as WINMUTEX_DIVERGENT and
          WINMUTEX_DIVERGENT_MEASURED_UTC; nothing refreshes them but a person.
       c. Nothing here owes LW, RC or CS an act. Round B is not RSC's work any
          more.
  2. THE SS AGE ITEM. ops/loop/slots.py short-circuits on AGE before pid_alive,
     so a LIVE holder past the 16200-second window is declared stale. This tree
     now runs a governed live daemon, so it can finally produce the hold()
     duration corpus it owes. RC, CS and LW owe theirs.
  3. THE RESPONDER STAYS DISABLED with three refusals and zero volunteers.
     ops/runtime/trial_confirmed.json records RC's REFUSAL and expired
     2026-09-08. NEVER write it - a gate one tree can satisfy alone is no gate.
  4. INBOX: 38 entries remain untriaged, named in the scratchpad file the ledger
     cites. 37 are RSC's own sent records; exactly ONE is inbound and is already
     answered. The watermark was deliberately NOT advanced.
  5. Open rows in ROADMAP.md, with what the round-B follow-up changed:
     - CLAUDE.md's directory-scoped phrasing: CLOSED. CLAUDE.md, README.md,
       ROADMAP.md and this file now state the FOUR pin carriers of
       ops/loop/slots.py and the FIVE of ops/loop/winmutex.py separately, each
       with the names and the 2026-09-20T23:41:45Z stamp.
     - The carrier constants that cannot redden for another tree: STILL OPEN,
       and it is a STATED LIMIT rather than a gap to close by reaching across
       trees. What changed is that each cross-tree row now carries its
       measurement instant, its sweep command and its swept root list, and the
       diagnostic quotes the instant - so an overtaken row reads as a dated
       record rather than as a false present tense. It still goes stale
       silently, and no arm can catch that.
     - The clean-index hole in the staged-diff check: STILL OPEN, untouched.
     - The range-only guard on the CLAUDE.md line citation: STILL OPEN, and it
       decayed twice more this session. See the trap entry above.
     - NEW, and the one to act on first: the citation guard's corpus is
       SUFFIX-KEYED and this file is excluded by it. See the header.

STANDING DIRECTIVES - THESE ARE NOT UP FOR RE-ASKING
  Nothing waits for operator approval. Adjudicate a contested call, act on the
  ruling, and report it as done. Commit and push everything. Subagent-first,
  always: the main session is the OPERATOR'S SURFACE. The producer NEVER grades
  its own work, and agreement between two agents is not evidence - find their
  shared input and test THAT. Emit an inline checklist at session start, again
  after at least four completions, and as the /done pre-flight - never per item.
  CAVEMAN ULTRA in chat; byte-exact everywhere else. The cross-repo inbox is
  reviewed, triaged into four buckets, ingested, implemented and ANSWERED every
  session, and delivery is verified by reading the RECIPIENT'S copy. Silence
  reads as DISSENT.
