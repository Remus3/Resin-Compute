RESINCOMPUTE - NEXT SESSION HAND-OFF
Stamped 2026-09-20, at commit d2a1bcf on main. Everything below is a READING
taken that day, not a promise about now. Re-measure before citing.

BOOTSTRAP - READ IN THIS ORDER
  CLAUDE.md
  README.md
  docs/SPEC_SCAFFOLD.md
  ROADMAP.md
  docs/LEDGER.md
  git log --oneline -10
Before planning any character build: docs/GOAL_SPEC_SEED_TEAM.md.
Before adding any data source: docs/LICENSE_NOTES.md.
Before re-litigating a past choice: docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS from memory or from a web search. They are
verified and recorded in docs/SPEC_SCAFFOLD.md section 3, with corrections in
docs/adr/ADR-003-forecaster-model.md. Three are regression-tested and easy to
get wrong: the 50/50 has been 55.000 percent consolidated since version 5.0,
not 50; the weapon soft-pity ramp saturates at pull 77 under a 7 percent
increment, so any claim of 79 or 80 is arithmetically impossible; and 1.600
percent is a long-run average, never a per-wish Bernoulli parameter.

THE GATES, in the order /done runs them
  python -m pytest tests/test_licence_posture.py -q
  python -m pytest tests/test_docs_consistency.py -q
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
mypy is ADVISORY, not a gate. It traverses only the files= roots in mypy.ini.
It says NOTHING about headless/, ops/, surface/, scripts/ or tests/, so never
cite its Success as evidence about code outside those roots. Never run
pytest . from the root; the two suites run separately.

STATE OBSERVED 2026-09-20 AT d2a1bcf
  tests 3003 passed 3 skipped; pity_engine 80 passed; shell node 52 passed;
  ruff clean; mypy 37 source files; headless smoke exit 0; licence 47;
  docs 34; qa_companion 17 passed 2 skipped 3 noted.
  Working tree clean, all work pushed.

WHAT LANDED THIS SESSION - five commits, 963cda1..d2a1bcf, all pushed
  67bd2fd  The inbox drop walker is pruned with a casefolded name match, given
           a depth bound, and the name table moved to one owner in
           core/walkprune.py shared by three call sites.
  8433eba  tools/publish_next_session.py converges the Desktop .lnk onto the
           tracked hand-off instead of writing a detached copy. A machine
           identity leak in a refusal detail was closed in the same commit.
  93898dd  CHANNEL_VERSION 2 vendored: docs/CHANNEL.md at 25425 bytes, 0 CR,
           LF sha256 fc22e86eebe93bb247a91f44835257a3fe717a287c4a3184a8e7a7b9a463fb9c,
           taken from origin/main as an independent witness. Thirteen pinned
           constructs moved; eight v1 literals are now derived.
  3f47b1a  tests/test_responder_refusal_gates_fire.py drives six refusal gates
           in process and asserts on the RESULT, beside the syntax census that
           a gate RELOCATION defeats.
  d2a1bcf  The duplicate syntax arm dropped, so the behavioural module stays
           inside the mutation campaign instead of being excluded whole.

CORRECTIONS MADE THIS SESSION - DO NOT RESURRECT EITHER
  1. "RC's commit is UNPUSHED" was the stated blocker on holding CHANNEL v2.
     FALSE. git branch -r --contains answers origin/main. The claim had simply
     DECAYED - RC's note said not-pushed as that note was written, and every
     reader since carried it forward unre-measured. A fact with a timestamp is
     not a fact with a lifetime.
  2. "RC's acceptance criterion can never be met." WITHDRAWN. The criterion is
     MEETABLE and not yet met. CS is the sole outstanding carrier at v1,
     899f6eb9 / 20633 bytes. LL's copy sits at a vendoring prefix under its
     third_party tree, which resolves RC's previously unexplained gap: the
     probe was not wrong, its predicate was too narrow.

THE FOUR-VERSUS-FIVE CARRIER DISPUTE IS RESOLVED AND WAS NEVER A DISAGREEMENT
  FOUR is the ops/loop/slots.py carrier population at 71fa2a68 - LW, RC, RSC,
  SS, with CS and LL absent. FIVE is the docs/CHANNEL.md population - CS, LL,
  LW, RC, RSC, with SS absent. The two exception sets are DISJOINT, which is
  why neither number ever corrected the other. Name the population before
  citing either figure.

TRAPS MEASURED IN THIS TREE - EACH ONE HAS ACTUALLY BITTEN
  - A GUARD WHOSE CORPUS COMES FROM git ls-files CANNOT SEE AN UNTRACKED FILE.
    This bit this session: a new test module passed the builder's suite run and
    the merge-seam run at 3004, then the pre-push gate REFUSED the commit,
    because the guard builds its corpus from git ls-files and the module was
    untracked. Both green runs were vacuous for that file. Remedy: git add a
    new file BEFORE running the seam gates.
  - THE PRE-PUSH HOOK REPORTS A DIFFERENT SKIP COUNT THAN AN INTERACTIVE RUN -
    4 rather than 3. That is EXPECTED, not a defect. git prepends its own PATH
    for hooks, so arms keyed on resolving a tool answer a different question
    there. It is documented in .githooks/pre-push. Do not file the two counts
    as a conflict.
  - A scheduled task reads Ready forever once its triggers expire. Prove
    liveness with ops/check_task_liveness.py and a future NextRunTime, never
    with the State string. State can VETO liveness and can never vouch for it.
  - schtasks invoked through Bash cannot see these tasks at all, so reach for
    the PowerShell cmdlets instead - but treat whatever they print as
    REPORTED and never as liveness, because that is the same trap again.
  - Path.write_text emits CRLF on Windows and eol=lf in .gitattributes hides it
    from every diff, so a clean-looking diff proves nothing. Read bytes, write
    bytes to a temp, replace, then verify the CR count on disk.
  - A quoted heredoc still mangles backslashes. Use the Write or Edit tool for
    content containing them. This has put a stray control byte into the ledger.
  - Under Git Bash a lone /F is rewritten to F:/ by MSYS path conversion, so
    write taskkill //F //PID <pid>. It fails SILENTLY when redirected.
  - Never Stop-Process on Windows.
  - A line-number citation into a doc decays the moment a file above it grows.
    tests/test_docs_consistency.py catches it; cite by heading or by text.
  - The docs guard REFUSES a backticked path that git does not track. Write
    gitignored and nonexistent paths in plain text, no backticks.
  - A commit message file must live in the SESSION SCRATCHPAD. A shared Git tmp
    path was overwritten by a concurrent session and a commit once landed
    carrying another tree's message. No gate exists for this.
  - A SHAPE ARM IS NOT A GUARD. An arm that reads a module's SOURCE TEXT for an
    absent name is defeated by a runtime-assembled name or by a second
    language. Assert on the ARTIFACT - a directory listing, a return value -
    not on what the source looks like.
  - Writing an outbound note into this tree's own inbox is AUTHORING, not
    DELIVERING. Verify by reading the RECIPIENT'S copy.
  - Subagents contradicted this session's briefs on measured points repeatedly
    and were right. Brief them to trust their own probe over the brief.

OPEN WORK, HIGHEST PRIORITY FIRST
  1. CS is the sole outstanding CHANNEL v2 carrier, still at v1. Blocked on CS,
     not on this tree. RSC has vendored and pushed.
  2. The joint re-pin round for ops/loop/slots.py and ops/loop/winmutex.py has
     NO AUTHOR AND NO DATE. Shape agreed by four trees; nobody has called it.
     Never edit either file unilaterally - both are byte-identical-by-contract
     and pinned by sha256, and the known-violation pin in
     tests/test_no_sibling_names.py goes RED when the carrier name is fixed,
     which is the prompt to re-pin inside that same round.
  3. The responder is DISABLED, not unregistered, and there are ZERO
     volunteers. ops/runtime/trial_confirmed.json EXISTS on disk, which invites
     the wrong reading: it records RC's REFUSAL, scope "LATENCY-ONLY only. RC
     is NOT armed.", and it EXPIRED at 2026-09-08T02:00Z. It is not a
     self-satisfied gate. NEVER write that file to unblock this tree - a gate
     one tree can satisfy alone is not a gate.
  4. The roster numeral is frozen in a test string literal in
     tests/test_channel_doc_pin.py with no mechanical tie to any roster list.
     A sixth participant was added by hand; a seventh would be missed the same
     way, with every arm green.
  5. ops/loop/slots.py short-circuits on AGE before pid_alive, so a LIVE holder
     past the window is declared stale. The window is 16200 seconds, 4h30m.
     Needs a measured hold() duration against it. RC, CS and LW owe that
     measurement; this tree's duration corpus is ZERO pairs.
  6. tests/test_loop_concurrency.py says "all three carriers" where the
     measured population is four. Tracked file, cosmetic but false.
  7. A GENERALISED finding, analysis only: a claim is defeatable whenever its
     BINDING CONDITIONS are narrower than its WORDING. Three measured
     instances are recorded in ROADMAP.md. Do not propose a CLAUDE.md change on
     that row's authority alone.

STANDING DIRECTIVES - THESE ARE NOT UP FOR RE-ASKING
  Nothing waits for operator approval. Adjudicate a contested call, act on the
  ruling, and report it as done. Commit and push everything, batching so CI
  waits overlap other work. Full machine access is granted with an adjudicator.
  Subagent-first, always: the main session is the operator's surface. Emit an
  inline checklist at session start, again only after at least four items have
  completed, and as the /done pre-flight - never per item. Report a time-to-
  completion for every action. CAVEMAN ULTRA in chat; byte-exact everywhere
  else. The cross-repo inbox is reviewed, triaged into four buckets, ingested,
  implemented and answered every session - silence reads as DISSENT.
