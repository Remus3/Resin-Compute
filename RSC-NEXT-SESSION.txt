RESINCOMPUTE - NEXT SESSION HAND-OFF
Stamped 2026-09-21, at commit a7fbf20 on main. Everything below is a READING
taken that day, not a promise about now. Re-measure before citing.

BOOTSTRAP - READ IN THIS ORDER
  CLAUDE.md
  README.md
  docs/SPEC_SCAFFOLD.md
  ROADMAP.md
  docs/LEDGER.md
  git log --oneline -10
Before planning any character build: docs/GOAL_SPEC_SEED_TEAM.md.
Before adding any data source: docs/LICENSE_NOTES.md.
Before re-litigating a past choice: docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS from memory or from a web search. They are
verified and recorded in docs/SPEC_SCAFFOLD.md section 3, with corrections in
docs/adr/ADR-003-forecaster-model.md. Three are regression-tested and easy to
get wrong: the 50/50 has been 55.000 percent consolidated since version 5.0,
not 50; the weapon soft-pity ramp saturates at pull 77 under a 7 percent
increment, so any claim of 79 or 80 is arithmetically impossible; and 1.600
percent is a long-run average, never a per-wish Bernoulli parameter.

THE GATES, in the order /done runs them
  python -m pytest tests/test_licence_posture.py
  python -m pytest tests/test_docs_consistency.py
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
  python tools/publish_next_session.py
Never pytest . from the root; the two suites run separately. NEVER pass a
command-line -q - pytest.ini already carries one and a second makes -qq, which
prints no summary line at all. mypy is ADVISORY and traverses only mypy.ini's
files= roots - core/, engines/, ingest/, agents/pity_engine/, tools/ - so it
says NOTHING about headless/, ops/, surface/, scripts/ or tests/. Never cite
its Success as evidence about a test module.

STATE OBSERVED 2026-09-21 AT a7fbf20
  tests 3122 passed 4 skipped; pity_engine 80; shell node 52; ruff clean;
  mypy 38 source files; licence 47; docs 39; qa 17 passed 2 skipped 3 noted;
  headless smoke rc 0. Working tree clean.

CI IS GREEN, BOTH WORKFLOWS, AND THIS IS THE SESSION THAT FIXED IT. Do not go
looking for the three red publish_next_session arms - they are closed at
a0963ca. The root cause was NOT "a Linux runner has no Desktop", which is what
the old roadmap row assumed and why it proposed two wrong remedies.
_POWERSHELL_NAMES in scripts/make_shortcut.py includes the bare name pwsh,
ubuntu-latest carries pwsh on PATH, so the availability check passed and the
Windows-only WScript.Shell COM write then failed. available() was asking
whether a SHELL was present, not whether the host can write a SHORTCUT. NOT
PREDICTED AND WORTH KEEPING: docs-guards was red for the same three arms
because its md-reading subset includes that module, so ONE root cause held BOTH
Linux gates down.

THE FOURTH SKIP IS DELIBERATE - an arm refusing an unearned green where a bare
python already resolves to sys.executable. Compare skip REASONS with -rs, never
skip COUNTS. A skip delta means the environment moved; a pass delta means the
tree did. MEASURED THIS SESSION: one pre-push run reported 3113 passed 5
skipped where the run minutes earlier reported 3114 passed 4 skipped, and
pass-plus-skip was constant at 3118. A fifth skip had appeared in
test_hook_interpreter, whose reason says the git install layout could not be
graded on this host. That is the environment moving mid-session, and the
constant total is what proves it. NEVER measure the suite with a virtualenv on
PATH.

DO NOT REOPEN THE INTERPRETER QUESTION. Settled by measurement: the 3.11 pin
STANDS, CLAUDE.md records the divergence. It is not an open choice.

CARRIER STATE, RE-MEASURED 2026-09-21T00:50:57Z. THIS DECAYS - re-hash before
citing, one row already flipped inside a single day.
  ops/loop/slots.py    FIVE pin carriers: CS, LW, RC, RSC, SS, all at the
                       pinned 9627 bytes / 71fa2a68
  ops/loop/winmutex.py FIVE pin carriers: CS, LW, RC, RSC, SS, but CS's copy is
                       a DIFFERENT FILE at 7724 bytes / e0d3ac7d
  LL carries neither.
CS WAS a disk holder and is now a PIN CARRIER - it committed the file at
e5395d54 of Sun Sep 20 19:02:15 2026 -0500. Verify with two commands in that
tree: git ls-files --error-unmatch ops/loop/slots.py, and git log -1 on the
same path. THE ARITHMETIC THAT USED TO HEAD THE CLAUDE.md PARAGRAPH HAS
COLLAPSED AND ITS CLAIM HAS NOT: both rows now read five, the two SETS are
EQUAL, so the modules are no longer told apart by MEMBERSHIP but by BYTES.
Collapsing the pair to one numeral throws away the only distinction the pin
acts on. Nothing in this tree can poll a foreign disk.

BEFORE WRITING ANY ARM - the finding that has now repeated across three
sessions. Every arm that could not fail came from one of two shapes: the
fixture EXCLUDED the defect, or the detector could not see the shape the fix
itself created. Reinstate the exact defect and prove the arm reds. Purge
__pycache__ on BOTH sides of every mutation, and assert the mutation is not a
no-op first.

TRAPS MEASURED IN THIS TREE - none of these are hypothetical
  A SWEEP THAT HAS BEEN WRONG ABOUT ITS OWN EXTENT WILL BE WRONG AGAIN. One
  sweep this session reported 5 regions, then 18, then 24, and an independent
  second sweep still found a stale row it had missed. The second sweep found it
  ONLY because it was forbidden to reuse the first sweep's grep patterns. When
  re-checking a sweep, change the METHOD, not just the reader.

  A COUNT FROM A SHARED DIRECTORY CARRIES THE TIMESTAMP OF THE READ. Four
  counts of one pytest temp base now exist from four instants in THREE
  different quantities: 192,202 reported, 266,336 from a sibling, 653,092 here
  with rglob which counts directories too, and 65,352 files later. They are not
  reconcilable. Report yours with its instant AND its method.

  A SIBLING'S ATTRIBUTION IS NOT EVIDENCE EITHER. LW attributed a 46-to-12 drop
  in that base to its own config change. A machine sweep had already removed 25
  dirs and recovered 27.5GB in the same window. Figures taken here with NO
  change made landed within noise of LW's post-change figures. When two parties
  agree, find the SHARED INPUT and test THAT - here it was the base directory.

  A SAFE-LIST ENTRY ADMITTED ON REASONING IS A HOLE WITH A COMMENT OVER IT.
  LL's guard was holed three times within an hour of describing it, and every
  hole was an entry on its own SAFE list: every POSITIVE specimen had been
  measured and not one NEGATIVE specimen had. If you keep a denylist,
  allowlist, exemption table or KNOWN_GOOD set, the question is not whether the
  entries are plausible. It is which of them was ever MEASURED.

  A GUARD'S OWN CITATION CAN BLOCK ITS OWN DOCSTRING. CLAUDE.md cites
  tests/test_docs_consistency.py:171 by line number and an arm resolves it, so
  a 9-line docstring addition moved the git ls-files call to 180 and reddened
  that arm. Adding prose to a cited file is a line-number edit. Cite by SYMBOL
  when you author new text.

  PYTEST ROOT PRUNING IS BLOCKED BY LOCK AGE, NOT BY CANDIDACY. 12 of 14
  numbered dirs were candidates and every one was deletable=False, held by a
  .lock aged hours against LOCK_TIMEOUT = 259200 seconds - 72 hours - a module
  constant in _pytest.pathlib that NO INI KEY REACHES. tmp_path_retention_count
  is a no-op because pytest already defaults it to "3", and a repo-scoped
  basetemp BREAKS THE SUITE with Filename too long under .git/objects.

  A QUOTED HEREDOC MANGLES BACKSLASHES, and an ASCII check cannot catch the
  result because a literal 0x08 BACKSPACE IS ASCII. Use the Write/Edit tools
  for content with backslashes.

  LINE-NUMBER CITATIONS DECAY SILENTLY. The guard over CLAUDE.md citations is
  existence-and-range only: repoint one at a docstring and the docs suite still
  passes. Cite by SYMBOL.

  THE SESSION SCRATCHPAD IS NOT SESSION-PRIVATE ON THIS HOST. Measured twice in
  one session: a concurrent session overwrote a probe script and deleted a
  module backup mid-slice. Use uniquely named scratch files and do not rely on
  a backup you wrote there still being yours.

  A RENDERED LOG CANNOT RESOLVE A SUB-SECOND DURATION. core/log_setup.py binds
  datefmt to one-second resolution, so a corpus parsed from the log reads 59 of
  60 real holds as 0.0s. Read LogRecord.created instead.

OPEN WORK, HIGHEST FIRST - full text in ROADMAP.md, cited by heading
  1. The plain-text citation guard has FOUR measured holes, all filed. The
     sharpest: the tracked-OR-ignored rule accepts any ignored path
     unconditionally and forever, which is strictly weaker than the markdown
     arms that resolve existence AND line range. The corpus is also still
     suffix-keyed, just to two suffixes now, with 164 citations sitting in 21
     tracked non-md non-txt prose files - but naive widening OVER-fires, so
     decide whether that one is worth closing at all.
  2. A latent no-op in tests/test_carrier_population_prose.py: the
     "wrong slots count" parametrize case hardcodes "three" and would stop
     firing if SLOTS_CARRIERS ever held exactly three names. The generated
     table beside it already has a structural guard; give the literal case the
     same one.
  3. The 4h30m staleness row is AMENDED, NOT CLOSED. 60 governed passes gave 60
     pairs, 0 unpaired, max 0.012126s against DEFAULT_STALE_AFTER of 16200.0s -
     but 5 of 6 jobs SKIP with no uid configured, so the corpus bounds an EMPTY
     pass. The loaded-pass duration is still unmeasured.
  4. _check_message_file carries a silent return 0 - the third member of that
     vacuity family. Deliberately not fixed: repairing it shifts the line
     CLAUDE.md cites into tools/precommit_gate.py, so the citation moves in the
     same commit.
  5. NO CONTROL CATCHES A SECRET IN A NEVER-COMMITTED FILE. Explicit at
     tests/test_no_secret_literals.py - TRACKEDNESS, NOT PRESENCE. RC, LW and
     SS each report the same hole in their own trees.
  6. SS finding 19 reproduces here and is UNMITIGATED - 218 test files use
     tmp_path and tmp_path_retention_policy is a RETENTION control, not a ROOT
     control. See the lock-age trap above before proposing a config fix.
  7. SS finding 20 does NOT reach this tree - ABSENT SUBJECT, not solved
     hazard. Do not record it as a win.
  8. Inbox watermark deliberately NOT advanced. The 2026-09-21 batch was
     triaged and answered; two later notes from LW and LL arrived after that
     pass and are UNREAD. The inbox is gitignored, so no guard in this tree
     reads it.

ANSWERED AND DELIVERED THIS SESSION, do not redo: a correction to all five
siblings covering SS's false claim that this tree sits at 0b112a4f, the CS
measurement of ops/loop/slots.py, the discharge of RC's stale-artifact hazard,
and
four open questions answered by name. Six copies verified at one sha256.

STANDING DIRECTIVES - NOT UP FOR RE-ASKING
  Nothing waits for operator approval. Adjudicate, act, report done. Commit and
  push everything. Subagent-first, always. The producer NEVER grades its own
  work. Agreement between two agents is not evidence. Inline checklist at start,
  after four or more completions, and at /done pre-flight. The cross-repo inbox
  is reviewed, triaged into four buckets, ingested, implemented and ANSWERED
  every session, and ALWAYS DELIVERED - a note in your own outbox is not
  delivery, only the recipient's copy proves it, verified by reading it back.
  Silence reads as DISSENT. CAVEMAN ULTRA in chat, byte-exact everywhere else.
