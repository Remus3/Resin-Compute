RESINCOMPUTE - NEXT SESSION HAND-OFF
Stamped 2026-09-20, at commit 517b259 on main. Everything below is a READING
taken that day, not a promise about now. Re-measure before citing.

BOOTSTRAP - READ IN THIS ORDER
  CLAUDE.md
  README.md
  docs/SPEC_SCAFFOLD.md
  ROADMAP.md
  docs/LEDGER.md
  git log --oneline -10
Before planning any character build: docs/GOAL_SPEC_SEED_TEAM.md.
Before adding any data source: docs/LICENSE_NOTES.md.
Before re-litigating a past choice: docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS from memory or from a web search. They are
verified and recorded in docs/SPEC_SCAFFOLD.md section 3, with corrections in
docs/adr/ADR-003-forecaster-model.md. Three are regression-tested and easy to
get wrong: the 50/50 has been 55.000 percent consolidated since version 5.0,
not 50; the weapon soft-pity ramp saturates at pull 77 under a 7 percent
increment, so any claim of 79 or 80 is arithmetically impossible; and 1.600
percent is a long-run average, never a per-wish Bernoulli parameter.

THE GATES, in the order /done runs them
  python -m pytest tests/test_licence_posture.py
  python -m pytest tests/test_docs_consistency.py
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
  python tools/publish_next_session.py
Never pytest . from the root; the two suites run separately. NEVER pass a
command-line -q - pytest.ini already carries one and a second makes -qq, which
prints no summary line at all. mypy is ADVISORY and traverses only mypy.ini's
files= roots - core/, engines/, ingest/, agents/pity_engine/, tools/ - so it
says NOTHING about headless/, ops/, surface/, scripts/ or tests/. Never cite
its Success as evidence about a test module.

STATE OBSERVED 2026-09-20 AT 517b259
  tests 3112 passed 4 skipped; pity_engine 80; shell node 52; ruff clean;
  mypy 38 source files; licence 47; docs 34; qa 17 passed 2 skipped 3 noted;
  headless smoke rc 0. Working tree clean.

THE FOURTH SKIP IS DELIBERATE - an arm refusing an unearned green where a bare
python already resolves to sys.executable. Compare skip REASONS with -rs, never
skip COUNTS. A skip delta means the environment moved; a pass delta means the
tree did. NEVER measure the suite with a virtualenv on PATH. Measured this
session: two worktrees off the same commit produced DIFFERENT skip reason sets -
one had sh on PATH and weaker ACL enforcement, the other the reverse. Neither
set was a superset, so the union was only covered across both.

DO NOT REOPEN THE INTERPRETER QUESTION. Settled by measurement: the 3.11 pin
STANDS, CLAUDE.md records the divergence. It is not an open choice.

ROUND B IS LANDED AND MERGED. DO NOT DISPATCH ANYONE AGAINST IT.
ops/loop/winmutex.py is 6184 bytes, sha256
df0a7a40c28818130dfde25144c971c06060b4645e5eb5f679fbdaf55e2e08d7, with the
carrier code removed from one comment line so it reads "Found on review,
2026-07-26." Landed at ef17cc8 together with every pin that depended on it, and
both halves of the coupling were PROVEN RED by running, not asserted.

CARRIER STATE, measured 2026-09-20T23:41:45Z. THIS DECAYS - re-hash before
citing, it already changed once inside a single session.
  LW  applied 6184/df0a7a40      RC  applied and pushed to its own origin
  SS  confirmed from its own disk, LAST CARRIER OUTSTANDING
  CS  divergent 7724/e0d3ac7d, a DIFFERENT file, owes nothing
  LL  carries neither shared file, voted rather than hashed
CS holds ops/loop/slots.py at the pinned 9627/71fa2a68 but UNTRACKED and not
ignored, so CS is a DISK holder and not a PIN carrier. Both halves verified.

BEFORE WRITING ANY ARM - the finding that repeated across two sessions now.
Every arm that could not fail came from one of two shapes: the fixture EXCLUDED
the defect, or the detector could not see the shape the fix itself created.
Reinstate the exact defect and prove the arm reds. Purge __pycache__ on BOTH
sides of every mutation, and assert the mutation is not a no-op first.

TRAPS MEASURED IN THIS TREE - none of these are hypothetical
  A KNOWN-VIOLATION PIN often doubles as the only proof a fixture was
  populated. Emptying one to [] was STRICTER for the arm that read it and made
  a DIFFERENT arm in the same module vacuous - it passed against empty files.
  Before emptying any pinned list, grep every arm that reads it.

  PYTHON \b TREATS UNDERSCORE AS A WORD CHARACTER, so a \b-delimited code
  scanner cannot see _RC_ or OWNER_RC_NOTE - the exact escape class it existed
  to catch. Closed with a separator boundary, 0 false positives measured.

  A QUOTED HEREDOC MANGLES BACKSLASHES. A slice this session wrote a literal
  0x08 BACKSPACE instead of \b that way, and an ASCII check cannot catch it
  because 0x08 IS ASCII. Use the Write/Edit tools for content with backslashes.

  A SUFFIX-KEYED GUARD HAS A SUFFIX-EXCLUDED DEFECT. tests/test_docs_consistency
  builds its citation corpus from _tracked_markdown(), which filters .md - so
  this file, being .txt, is outside it entirely and its citations are unguarded.

  LINE-NUMBER CITATIONS DECAY SILENTLY. Five in one dispatch brief were wrong
  this session. The guard over CLAUDE.md citations is existence-and-range only:
  repoint one at a docstring and the docs suite still passes 34. Cite by SYMBOL.

  A CORPUS IS A SNAPSHOT. A carrier tuple measured and verified by an
  independent pass was falsified by foreign disks moving. Stamp every
  cross-tree row with the instant and the command, so staleness is self-evident.

  AGREEMENT BETWEEN TWO AGENTS IS NOT EVIDENCE. Two agents reported the same
  carrier state this session from one stale read. Find the shared input, test
  THAT. Four trees sharing one guard hole shared an inherited TRIGGER SET, not
  a regex - population falls out of trigger before any predicate exists.

  A RENDERED LOG CANNOT RESOLVE A SUB-SECOND DURATION. core/log_setup.py binds
  datefmt at :228 and :260 to one-second resolution, so a corpus parsed from
  the log reads 59 of 60 real holds as 0.0s. Read LogRecord.created instead.

CI IS RED AND HAS BEEN SINCE BEFORE THIS SESSION. Measured at 694cfbf: 3 failed,
3093 passed, 20 skipped. The same three failed at 08f1de7 with 3073 passed, so
this session added 20 passing arms on Linux and introduced NOTHING. The three
are test_the_real_shell_writes_a_shortcut_that_resolves_to_the_source,
test_a_real_publish_leaves_the_shortcut_and_nothing_else and
test_no_report_or_refusal_names_a_path - all publish_next_session shortcut arms,
all raising Refusal: write_failed because a Linux runner has no Desktop to write
a shortcut to. They pass on Windows. This is a PLATFORM-DIVERGENT arm, not a
broken publisher: the local gate cannot see it because the local gate is the
platform where it works. FIX THIS FIRST NEXT SESSION - a permanently red CI is
an alarm nobody reads, and it is the only Linux gate this tree has.

OPEN WORK, HIGHEST FIRST - full text in ROADMAP.md, cited by heading
  1. The 4h30m staleness row is AMENDED, NOT CLOSED. 60 governed passes gave 60
     pairs, 0 unpaired, max 0.012126s against DEFAULT_STALE_AFTER of 16200.0s -
     but 5 of 6 jobs SKIP with no uid configured, so the corpus bounds an EMPTY
     pass. The loaded-pass duration is still unmeasured. Corpus at
     ops/runtime/slot_hold_corpus_2026-09-20.txt, which is gitignored.
  2. SEAM REQUEST, filed and not implemented: widen the citation corpus past
     .md so this file is guarded. Blast radius measured at three tracked .txt
     files, two of them requirements files with no citations.
  3. _check_message_file carries the SAME silent return 0 - the third member of
     that vacuity family. Deliberately not fixed: repairing it shifts the line
     CLAUDE.md cites into tools/precommit_gate.py, so the citation moves in the
     same commit.
  4. NO CONTROL CATCHES A SECRET IN A NEVER-COMMITTED FILE. Explicit at
     tests/test_no_secret_literals.py - TRACKEDNESS, NOT PRESENCE. Two other
     trees independently report the same hole.
  5. SS finding 19 reproduces here and is UNMITIGATED - the pytest temp root has
     42 children, PYTEST_DEBUG_TEMPROOT unset, 218 test files using tmp_path.
     tmp_path_retention_policy is a RETENTION control, not a ROOT control.
  6. SS finding 20 does NOT reach this tree - ABSENT SUBJECT, not solved hazard.
     Do not record it as a win.
  7. Inbox at 197 unread, population = entries absent from
     ops/runtime/inbox_seen.json. Watermark deliberately NOT advanced. The
     triage table is in moon_sync_inbox, which is gitignored.

STANDING DIRECTIVES - NOT UP FOR RE-ASKING
  Nothing waits for operator approval. Adjudicate, act, report done. Commit and
  push everything. Subagent-first, always. The producer NEVER grades its own
  work. Agreement between two agents is not evidence. Inline checklist at start,
  after four or more completions, and at /done pre-flight. The cross-repo inbox
  is reviewed, triaged into four buckets, ingested, implemented and ANSWERED
  every session, and ALWAYS DELIVERED - a note in your own outbox is not
  delivery, only the recipient's copy proves it, verified by reading it back.
  Silence reads as DISSENT. CAVEMAN ULTRA in chat, byte-exact everywhere else.
