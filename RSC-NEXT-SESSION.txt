RESINCOMPUTE - NEXT SESSION HAND-OFF
Stamped 2026-09-20, at commit d3a963d on main. Everything below is a READING
taken that day, not a promise about now. Re-measure before citing.

BOOTSTRAP - READ IN THIS ORDER
  CLAUDE.md
  README.md
  docs/SPEC_SCAFFOLD.md
  ROADMAP.md
  docs/LEDGER.md
  git log --oneline -10
Before planning any character build: docs/GOAL_SPEC_SEED_TEAM.md.
Before adding any data source: docs/LICENSE_NOTES.md.
Before re-litigating a past choice: docs/adr/README.md.

DO NOT RE-DERIVE GACHA CONSTANTS from memory or from a web search. They are
verified and recorded in docs/SPEC_SCAFFOLD.md section 3, with corrections in
docs/adr/ADR-003-forecaster-model.md. Three are regression-tested and easy to
get wrong: the 50/50 has been 55.000 percent consolidated since version 5.0,
not 50; the weapon soft-pity ramp saturates at pull 77 under a 7 percent
increment, so any claim of 79 or 80 is arithmetically impossible; and 1.600
percent is a long-run average, never a per-wish Bernoulli parameter.

THE GATES, in the order /done runs them
  python -m pytest tests/test_licence_posture.py -q
  python -m pytest tests/test_docs_consistency.py -q
  python scripts/qa_companion.py
  python -m ruff check .
  python -m pytest tests
  python -m pytest agents/pity_engine
  cd shell && node --test
  python -m headless.runner --once --dry-run
mypy is ADVISORY, not a gate. It traverses only the files= roots in mypy.ini.
It says NOTHING about headless/, ops/, surface/, scripts/ or tests/, so never
cite its Success as evidence about code outside those roots. Never run
pytest . from the root; the two suites run separately.

STATE OBSERVED 2026-09-20 AT d3a963d
  tests 2949 passed 3 skipped; pity_engine 80 passed; shell node 52 passed;
  ruff clean; headless smoke exit 0; three QA gates green.
  Working tree clean, all work pushed.

WHAT LANDED THIS SESSION
  14a109b  CR arm and a scoped channel-code pin over the two vendored governor
           files. Neither shared file was touched.
  1a6d8da  run_daemon now holds a machine-wide lane slot around each LIVE pass.
           This repository is a REAL acquirer for the first time.
  853e9a0  Retired the inert-governor prose across six files; withdrew a false
           safety claim; recorded the responder arming.
  d3a963d  Responder task disabled after two refusals.

FIVE CLAIMS RETRACTED ON 2026-09-20 - DO NOT RESURRECT ANY OF THEM
  1. "is_stale is GUARDED by a documented mtime fallback." FALSE. The mtime arm
     is reached only when _read is falsy, and the neutralising write is IN
     PLACE, so tearing it sets mtime to the moment of the tear. Measured: _read
     empty, age 0.00034s, is_stale False, reap removed 0, lockfile still on
     disk. It reads NOT stale for the next 4.5 hours. The claim originated at a
     sibling and was carried in this tree's own tracked ROADMAP until refuted.
  2. "slots.hold() reaps before acquiring." FALSE. try_acquire runs first and
     reap runs only after it fails, so reaping happens under contention alone.
  3. "This repository contributed nine files to the git installation root."
     It is SIX, 10889 bytes. The largest file claimed, 170803 bytes, belongs to
     another carrier. A widened pattern gives seven and 20548 bytes; the extra
     file names four trees and is attributable to none.
  4. "Unbounded log growth justifies disabling the responder." FALSE. The
     invocation log is capped and trimmed, guarded by
     tests/test_responder_invocation_trim.py. The real reason is that the
     counterparty gate is the FIRST gate in _run_once, so a fire produced two
     log lines and nothing else.
  5. An adjudicator returned a per-code measurement table having made ZERO tool
     calls. Every command and number in it was invented. A read-only agent's
     tool-use count is a checkable property of its report.

TRAPS MEASURED IN THIS TREE - EACH ONE HAS ACTUALLY BITTEN
  - A scheduled task reads Ready forever once its triggers expire. Prove
    liveness with ops/check_task_liveness.py and a future NextRunTime, never
    with the State string. State can VETO liveness and can never vouch for it.
  - schtasks invoked through Bash cannot see these tasks at all, so reach for
    the PowerShell cmdlets instead - but treat whatever they print as
    REPORTED and never as liveness, because that is the same trap again.
    Liveness comes only from the exit code of ops/check_task_liveness.py.
  - A quoted heredoc still mangles backslashes. Use the Write or Edit tool for
    content containing them. This has put a stray control byte into the ledger.
  - Under Git Bash a lone /F is rewritten to F:/ by MSYS path conversion, so
    write taskkill //F //PID <pid>. It fails SILENTLY when redirected.
  - Never Stop-Process on Windows.
  - A line-number citation into a doc decays the moment a file above it grows.
    tests/test_docs_consistency.py catches it; re-derive rather than revert.
  - The docs guard REFUSES a backticked path that git does not track. Write
    gitignored and nonexistent paths in plain text, no backticks.
  - A commit message file must live in the SESSION SCRATCHPAD. A shared Git tmp
    path was overwritten by a concurrent session and a commit once landed
    carrying another tree's message. No gate exists for this.
  - Writing an outbound note into this tree's own inbox is AUTHORING, not
    DELIVERING. Verify by reading the RECIPIENT'S copy.
  - Subagents contradicted this session's briefs on measured points at least
    six times and were right every time. Brief them to trust their own probe.

OPEN WORK, HIGHEST PRIORITY FIRST
  1. CHANNEL_VERSION 2, six-participant roster. RC accepted authorship. This
     tree voted YES and IS a pin-holder: docs/CHANNEL.md tracked, 20633 bytes,
     0 CR, LF-normalised sha256 899f6eb957cc26ee25993d83d65d8ca291841fe4eec24a48f729c2dc005f4c6b.
     Five-ness is asserted in 15 places and three are NOT simple substitutions.
     Blocked on RC, not on this tree.
  2. A carrier name sits in the shared winmutex module at line 118 while the
     shared slots module says nothing may reference any of them. INERT - no
     value, no behaviour, no byte-identity break. Fix belongs to a JOINT re-pin
     round across every carrier, never a unilateral edit. The known-violation
     pin in tests/test_no_sibling_names.py goes RED when it is fixed, which is
     the prompt to update the pin in that same round.
  3. A LATENT unpruned walker at scripts/watch_inbox.py:1040 - every child
     directory is appended with no skip set, reached from a per-prompt hook,
     and its caller hashes every file returned. Latent only because the inbox
     holds zero directories and MAX_DROP_ENTRIES budgets the walk.
  4. The responder re-arms the moment any tree volunteers as a counterparty.
     The task is DISABLED, not unregistered, so one Enable plus a window move
     restores it. NEVER write ops/runtime/trial_confirmed.json to unblock this
     tree - a gate one tree can satisfy alone is not a gate, and two trees have
     now explicitly declined.
  5. tools/publish_next_session.py still writes a detached Desktop COPY. It
     must be converted to converge the shortcut instead. Until then /done runs
     only its --check mode.

STANDING DIRECTIVES - THESE ARE NOT UP FOR RE-ASKING
  Nothing waits for operator approval. Adjudicate a contested call, act on the
  ruling, and report it as done. Commit and push everything, batching so CI
  waits overlap other work. Full machine access is granted with an adjudicator.
  Subagent-first, always: the main session is the operator's surface. Emit an
  inline checklist at session start, again only after at least four items have
  completed, and as the /done pre-flight - never per item. Report a time-to-
  completion for every action. CAVEMAN ULTRA in chat; byte-exact everywhere
  else. The cross-repo inbox is reviewed, triaged into four buckets, ingested,
  implemented and answered every session - silence reads as DISSENT.
